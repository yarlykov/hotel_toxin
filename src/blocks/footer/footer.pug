include ../../blocks/links/links.pug
include ../../blocks/subscribe/subscribe.pug
include ../../blocks/logo/logo.pug
include ../../blocks/social/social.pug

mixin footer({ logo={}, footerText="", footerNavigation={}, subscribe={}, copyright={}, social={} })
  footer.footer__wrapper
    .footer 
      .footer__logo
        +logo(logo)
        p.footer__award #{footerText}
      .footer__links
        .footer__links_column
          +links(footerNavigation.column1)
        .footer__links_column
          +links(footerNavigation.column2)
        .footer__links_column
          +links(footerNavigation.column3)
      +subscribe(subscribe)
    .footer__bottom
      .footer__bottom_wrapper
        .footer__copyright-text #{copyright.text}
        .footer__social
          +social(social)
  footer.footer-mobile
    .footer-mobile__wrapper
      +logo(logo)
      .footer-mobile__copyright-text #{copyright.text}
      .footer-mobile__social
        +social(social)