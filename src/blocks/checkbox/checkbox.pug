mixin checkbox({ mainTitle="", dropdown=false, rich=false, listItems=[]})

  -
    let titleClassName = "checkbox__title"
    let descriptionClassName = "checkbox__description"
    titleClassName += rich ? "_rich" : ""
    descriptionClassName += rich ? "_rich" : ""

  .checkbox
    .checkbox__header
      div.checkbox__header_title.font__h3 #{mainTitle}
      if dropdown
        .checkbox__header_arrow-down
    each item in listItems
      label.checkbox__item
        input&attributes({
          class: "checkbox__input",
          type: "checkbox",
          title: item.title,
          checked: item.checked
        })
        span.checkbox__box
        div(class = titleClassName) #{item.title}
          if rich
            div(class = descriptionClassName) #{item.description}